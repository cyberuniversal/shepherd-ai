# Processed Results

This directory contains promoted, machine-readable study outputs. Raw model
text remains in the committed checkpoint archives under
`datasets/multiuav_plat/nautilus/`; derived analysis is stored separately here.

Each promoted result is tied to a frozen input, run configuration, checksum, or
provenance manifest. Do not overwrite admitted artifacts in place.
